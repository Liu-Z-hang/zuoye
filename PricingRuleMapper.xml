<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.studyroomserver.mapper.PricingRuleMapper">

    <select id="selectOrderByPriority" resultType="com.example.studyroomserver.entity.PricingRule">
        SELECT * FROM pricing_rule WHERE deleted = 0 ORDER BY priority DESC
    </select>

</mapper>